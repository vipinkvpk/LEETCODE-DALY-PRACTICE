class Solution:
    def minElement(self, nums: List[int]) -> int:
        # sum=0
        # sum_list
        # for i in str(nums):
        #     sum=in
        return min(sum(map(int,str(num))) for num in nums)

        